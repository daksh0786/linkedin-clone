import styled from "styled-components";
import classes from "./Login.module.css"; // Corrected import statement

const Login = (props) => {
  return (
    <div>
      <nav className={classes.ima}>
        <a href=" ">
          <img src="/Images/login-logo.svg" alt=" "></img>
        </a>
        <div>
          
        </div>
      </nav>
    </div>
  );
};
export default Login;
